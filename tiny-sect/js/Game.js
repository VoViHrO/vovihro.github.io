import Renderer from './Renderer.js';import World from './world/World.js';export default class Game{constructor(){this.canvas=document.getElementById('gameCanvas');this.ctx=this.canvas.getContext('2d');this.renderer=new Renderer(this);this.world=new World();this.last=0;this.resize();addEventListener('resize',()=>this.resize());this.canvas.onclick=e=>this.click(e)}resize(){this.canvas.width=innerWidth;this.canvas.height=innerHeight-168}start(){requestAnimationFrame(t=>this.loop(t))}loop(t){this.world.update((t-this.last)/1000);this.last=t;this.renderer.render();requestAnimationFrame(x=>this.loop(x))}click(e){const r=this.canvas.getBoundingClientRect();this.world.selectAt(e.clientX-r.left,e.clientY-r.top)}}